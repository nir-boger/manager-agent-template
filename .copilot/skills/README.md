# Runner scripts for DM sprint skills
#
# Each scheduled task calls one of these .ps1 files. The script invokes
# copilot in non-interactive (programmatic) mode, pointing it at the
# corresponding SKILL.md for full instructions.
#
# Required: copilot must be on PATH and already authenticated.
# Required: az CLI logged in (ADO MCP server uses it).
